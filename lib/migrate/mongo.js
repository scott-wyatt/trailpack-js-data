/* eslint no-console: [0, { allow: ["log","warn", "error"] }] */
'use strict'

module.exports = {
  /**
   * sync Mongo Models for connection
   * @param {Object} store JSData store
   * @param {object} connection
   * @param {Boolean} force
   */
  sync (definitions, connection, force){

  }
}
